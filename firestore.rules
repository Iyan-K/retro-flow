rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId}/posts/{postId} {
      // Development only – replace with authentication checks before production.
      allow read, write: if true;
    }
  }
}
